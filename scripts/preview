#!/bin/sh
set -eu

repo_dir=$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)
cd "$repo_dir"

printf 'Compilation/build timing (seconds):\n'
/usr/bin/time -p cargo build --quiet --bin preview

cargo run --quiet --bin preview -- "$@"
